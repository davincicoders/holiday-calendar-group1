<% today.step(in_a_month) do |date| %>
	<% if date.day != HolidayFields.where(holidate: date.day).first %>
  	<%= date.day %>
	<% else %>
		<%= date.day %><br>
		<%= HolidayFields.where(holidate: date.day).first.name %>
	<% end %>
<% end %>